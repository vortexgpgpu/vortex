Assignments from the past 24 hours (2026-04-06 to 2026-04-07)
=============================================================

1. Investigate why SimX IPC is much worse than RTL by analyzing generated traces:
   $ ci/blackbox.sh --driver=rtlsim --app=vecadd --debug=3 --perf=1 --log=run_rtlsim.log
   $ ci/blackbox.sh --driver=simx --app=vecadd --debug=3 --perf=1 --log=run_simx.log
   Status: DONE — identified 5 findings (CTA dispatch asymmetry, memory latency,
   stall modeling, branch count mismatch, warp scheduling)

2. (Finding 1) CTA dispatch asymmetry (5/5/5/1 RTL vs 4/4/4/4 SimX) — who is wrong?
   Status: DONE — RTL is wrong (ring buffer admission bug)

3. (Finding 4) Branch count mismatch (32 RTL vs 16 SimX) — investigate and fix the wrong one
   Status: DONE — SimX was missing JAL branch count. Fixed in execute.cpp.

4. (Finding 5) Warp scheduling — comparing with RTL, should be same, right?
   Status: DONE — confirmed both use priority encoder (lowest wid first)

5. Propose a RTL fix for CTA dispatch asymmetry (finding 1)
   Status: DONE — applied fix to VX_cta_dispatch.sv (table_notfull -> tail_slot_free)
   BUT: user reverted it — broke rtlsim. Instructed: do NOT change it again.

6. SimX is still too slow compared to RTL — open run_rtlsim.log and run_simx.log,
   track down the latency of each instruction, match the UUID in both files,
   and identify which pipeline stage is timed incorrectly in SimX.
   Status: DONE — root cause: SimX sets fetch_stall=true for LSU instructions,
   blocking the warp until load commit. RTL's VX_decode.sv does NOT set is_wstall
   for LSU — warp is free to continue scheduling while loads are in-flight.

7. Add same is_wstall() to Instr class (matching RTL VX_decode.sv)
   Status: DONE — added is_wstall_ field, set_wstall(), is_wstall() to Instr

8. Change to stored field pattern: instr.set_wstall() called in decode, instr.is_wstall() getter
   Status: DONE

9. Refactor all Instr methods to snake_case:
   setFUType->set_fu_type, getOpType->get_op_type, setMacroOp->set_macro_op,
   getSrcReg->get_src_reg, getTmask->get_tmask, hasTmask->has_tmask, etc.
   Status: DONE — updated instr.h, decode.cpp, execute.cpp, emulator.cpp,
   core.cpp, sequencer.cpp, tensor_unit.cpp, vec_unit.cpp

10. Fix broken test:
    CONFIGS="-DNUM_THREADS=8 -DEXT_TCU_ENABLE -DTCU_WGMMA_ENABLE -DITYPE=int8
    -DOTYPE=int32 -DWGMMA_NRC=8" ./ci/blackbox.sh --driver=simx --app=sgemm_tcu_wg
    Status: DONE — TCU instructions needed is_wstall=true (macro-ops must stall
    warp until sequencer finishes expanding micro-ops)

11. Move wstall logic from Emulator::decode(instr_trace_t*) into
    Instr::Ptr Emulator::decode(uint32_t code, ...) and set inside switch statement
    Status: DONE — set_wstall(true) added per instruction type in decode.cpp switch,
    emulator.cpp now just does trace->fetch_stall = instr->is_wstall()

12. (From prior session, referenced but not in current context):
    Add shared TCU accumulator, K-step gating
    Status: UNKNOWN — work was done in a prior conversation that ran out of context.
    Details not available in current memory.
