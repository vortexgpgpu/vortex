ROOT_DIR := $(realpath ../../..)
include $(ROOT_DIR)/config.mk

PROJECT := dxa_tma_wgmma_s2g_pipeline
SRC_DIR := $(VORTEX_HOME)/tests/regression/$(PROJECT)
WGMMA_NRC ?= 16
TMA_PIPE_K_TILES ?= 2
TMA_PIPE_ITERS ?= 8
TMA_PIPE_IN_STAGES ?= 3
TMA_PIPE_OUT_STAGES ?= 3
TMA_PIPE_STATE_STAGES ?= 3
TMA_PIPE_META_WORDS ?= 8

CONFIGS += -DVX_CFG_EXT_TCU_ENABLE -DVX_CFG_TCU_WGMMA_ENABLE -DWGMMA_RS -DWGMMA_NRC=$(WGMMA_NRC)
CONFIGS += -DTMA_PIPE_K_TILES=$(TMA_PIPE_K_TILES) -DTMA_PIPE_ITERS=$(TMA_PIPE_ITERS)
CONFIGS += -DTMA_PIPE_IN_STAGES=$(TMA_PIPE_IN_STAGES) -DTMA_PIPE_OUT_STAGES=$(TMA_PIPE_OUT_STAGES) -DTMA_PIPE_STATE_STAGES=$(TMA_PIPE_STATE_STAGES)
CONFIGS += -DTMA_PIPE_META_WORDS=$(TMA_PIPE_META_WORDS)
CONFIGS += -DVX_CFG_EXT_DXA_ENABLE -DVX_CFG_EXT_DXA_GROUP_ENABLE -DVX_CFG_EXT_DXA_S2G_ENABLE -DVX_CFG_L2_ENABLE
CONFIGS += -DVX_CFG_NUM_THREADS=8 -DVX_CFG_NUM_WARPS=8 -DVX_CFG_NUM_BARRIERS=32 -DVX_CFG_ISSUE_WIDTH=2 -DVX_CFG_NUM_DXA_CORES=1

SRCS := $(SRC_DIR)/main.cpp
VX_SRCS := $(SRC_DIR)/kernel.cpp
HDRS := $(SRC_DIR)/common.h
VX_HDRS := $(HDRS)
KERNEL_LIB := vortex2
include ../common.mk
