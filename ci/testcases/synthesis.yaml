# Yosys synthesis smoke (host). Native catalog entries — replaces the legacy
# regression.sh --synthesis delegation. CONFIGS flows to the make via env.
category: synthesis
defaults:
  xlen: [32, 64]
  tier: nightly
tests:
  - id: base
    via: script
    run: "make -C hw/syn/yosys"
