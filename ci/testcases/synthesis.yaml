# Phase-D category: delegates to the legacy ci/regression.sh --synthesis (multi-step
# build + host/python runner). Expand into native specs in a later pass.
category: synthesis
defaults:
  xlen: [32, 64]
  tier: nightly
tests:
  - id: legacy
    via: script
    run: "./ci/regression.sh --synthesis"
