# Test cases for `runtime`, drafted from regression.sh.in (review/collapse).
category: runtime
defaults:
  xlen:
  - 32
  - 64
  tier: fast
tests:
- id: isa-1
  via: make-run
  drivers:
  - simx
  dir: tests/runtime
  target: run-{driver}
