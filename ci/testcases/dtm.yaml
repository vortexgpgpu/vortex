# Phase-D category: delegates to the legacy ci/regression.sh --run dtm (multi-step
# build + host/python runner). Expand into native specs in a later pass.
category: dtm
defaults:
  xlen: [32, 64]
  tier: slow
tests:
  - id: legacy
    via: script
    run: "./ci/regression.sh --run dtm"
    needs: [dtm]
