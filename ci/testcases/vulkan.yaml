# Test cases for `vulkan`, drafted from regression.sh.in (review/collapse).
category: vulkan
defaults:
  xlen:
  - 32
  - 64
  tier: fast
tests:
- id: isa-1
  via: make-run
  drivers:
  - simx
  dir: tests/vulkan
  target: run-{driver}
- id: isa-2
  via: make-run
  drivers:
  - rtlsim
  dir: tests/vulkan
  target: run-{driver}
- id: isa-3
  via: make-run
  drivers:
  - opae
  dir: tests/vulkan
  target: run-{driver}
- id: isa-4
  via: make-run
  drivers:
  - xrt
  dir: tests/vulkan
  target: run-{driver}
