# Phase-D category: delegates to the legacy ci/regression.sh --cupbop (multi-step
# build + host/python runner). Expand into native specs in a later pass.
category: cupbop
defaults:
  xlen: [64]
  tier: slow
tests:
  - id: legacy
    via: script
    run: "./ci/regression.sh --cupbop"
